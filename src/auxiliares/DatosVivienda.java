package auxiliares;

import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;
import java.util.ArrayList;

import clases.JefeNucleo; 
import clases.Vivienda;
import auxiliares.DatosJefeNucleo;

public class DatosVivienda {
/*
	    public static ArrayList<Vivienda> cargarDesdeTXT(String ruta) throws IOException {
	    	ArrayList<Vivienda> viviendas = new ArrayList<Vivienda>();
	        try (BufferedReader reader = new BufferedReader(new FileReader(Datos_Viviendas.txt))) {
	            String linea;
	            while ((linea = reader.readLine()) != null) {
	                String[] datos = linea.split(",");
	                viviendas.add(new Vivienda(datos[0], Integer.parseInt(datos[1]), datos[2]));
	            }
	        }
	        return usuarios;
	    }
		
	}*/
}
